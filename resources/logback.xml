<configuration>

    <!-- Consola normal para TU app -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{HH:mm:ss}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- (Opcional) Fichero solo para Hibernate -->
    <appender name="HIBERNATE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/hibernate.log</file>
        <encoder>
            <pattern>[%d{HH:mm:ss}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger de Hibernate: nada de DEBUG/INFO en consola -->
    <logger name="org.hibernate" level="WARN" additivity="false">
        <appender-ref ref="HIBERNATE_FILE"/>
    </logger>

    <!-- Tu código: consola bonita -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
